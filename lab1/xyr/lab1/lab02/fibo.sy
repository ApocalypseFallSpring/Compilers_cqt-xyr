int add(int a, int b) {
    return a + b;
}

int main() {
    int a = 0;
    int b = 1;
    int i = 1;
    int t;
    int n;
    n = getint();
    
    putint(a);
    putch(10);  // 输出换行符
    putint(b);
    putch(10);
    
    while (i < n) {
        t = b;
        b = add(a, b);  // 保留add函数调用
        if (b % 2 == 0) {
            putint(b);
            putch(10);
        }
        a = t;
        i = i + 1;
    }
    return 0;
}